# RakshaNet Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# DATABASE CONFIGURATION
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_password_in_production
POSTGRES_DB=upi_sentinel_db
DB_PORT=5432

# ============================================
# NODE.JS BACKEND CONFIGURATION
# ============================================
NODE_ENV=production
API_PORT=3000
CORS_ORIGIN=*

# Database connection (for Docker use service name)
DB_HOST=db

# ML Service URL (for Docker use service name)
ML_API_URL=http://ml_engine:8000

# ============================================
# PYTHON ML ENGINE CONFIGURATION
# ============================================
ML_PORT=8000
MODEL_PATH=/app/upi_fraud_risk_models.pkl

# ============================================
# NGINX CLIENT CONFIGURATION
# ============================================
CLIENT_PORT=80

# ============================================
# NOTES
# ============================================
# - For production, use strong passwords
# - Set CORS_ORIGIN to your frontend domain in production
# - All services communicate via Docker network using service names
# - Environment variables are automatically loaded by docker-compose
